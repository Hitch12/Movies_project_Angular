import { SolveImagePipe } from './solve-image.pipe';

describe('SolveImagePipe', () => {
  it('create an instance', () => {
    const pipe = new SolveImagePipe();
    expect(pipe).toBeTruthy();
  });
});
